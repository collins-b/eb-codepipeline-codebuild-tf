apiVersion: v1
kind: Service
metadata:
  name: ebtf
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: ebtf
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ebtf
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: ebtf
  template:
    metadata:
      labels:
        app: ebtf
    spec:
      containers:
        - name: ebtf
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 3000
